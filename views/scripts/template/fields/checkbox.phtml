<input
    type="checkbox"
    rel="${_elemName}"

    {{if (_fieldValue)}}
        checked="checked"
    {{/if}}

    {{each(idx,attrValue) _dataConfig.attributes}}
        ${idx}="${attrValue}"
    {{/each}}

/>
<input
    type="hidden"
    id="${_elemIden}"
    name="${_elemName}"
    value="${_fieldValue}"
    class="checkbox"
/>
