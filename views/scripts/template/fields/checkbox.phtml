<input
    type="checkbox"
    rel="${_elemName}"

    {{if (_fieldValue)}}
        checked="checked"
    {{/if}}
/>
<input
    type="hidden"
    id="${_elemIden}"
    name="${_elemName}"
    value="${_fieldValue}"
    class="checkbox"
/>
