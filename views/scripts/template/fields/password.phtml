<span><input type="checkbox" rel="${_elemName}" /><?php echo $this->translate('Change password'); ?></span>
<br />
<input
    type="password"
    class="password"
    name="${_elemName}"
    id="${_elemIden}"
    value="${_fieldValue}"
    {{each(idx,attrValue) _dataConfig.attributes}}
        ${idx}="${attrValue}"
    {{/each}}
/>
